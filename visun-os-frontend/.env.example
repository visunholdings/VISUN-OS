# Sao chép file này thành .env.local rồi điền giá trị thật khi đã có project Supabase (xem visun-os-backend/README.md).
# Để trống -> app tiếp tục chạy ở chế độ demo (localStorage) như hiện tại, không có gì thay đổi.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
